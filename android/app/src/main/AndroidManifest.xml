<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Network is needed only to reach the KAIDO runtime. There are no other
         permissions in this first shell. Notably absent, on purpose:
           - INTERNET is the only one requested.
           - No CAMERA, no RECORD_AUDIO, no READ_CONTACTS, no location, and no
             BIND_NOTIFICATION_LISTENER_SERVICE.
         Those arrive in later shells, each behind its own explicit user grant,
         because KAIDO never takes a capability it has not shown the user. -->

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.KAIDO"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
